.codex/

/target

# Local planning artifacts
/plans/

# Ralph artifacts
*ralph*
prd.json

# Upstream XrayDB repo (cloned for data generation)
# Note: on case-insensitive macOS, /XrayDB also matches /xraydb
/XrayDB

# Intermediate data files
/data/xraydb_raw.json

# Synced copy of the root README (kept in the package so include_str!/cargo publish work).
# Regenerate with: cp README.md xraydb-lib/README.md

# wasm-pack output
/web/pkg

# Database blob served to the demo (copied from xraydb-lib/data)
/web/data
